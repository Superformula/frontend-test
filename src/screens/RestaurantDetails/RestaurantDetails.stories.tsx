import React from 'react';
import { storiesOf } from '@storybook/react';

import { RestaurantDetails } from './RestaurantDetails';

storiesOf('RestaurantDetails screen', module).add('Default', () => <RestaurantDetails />);
